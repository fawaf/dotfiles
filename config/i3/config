# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Mod1 is alt key
# Mod4 is super/windows key
# keycode 66 is cap lock key
set $mod Mod4
set $alt Mod1
set $ctrl Control
set $shift Shift

set $lock xsecurelock

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1

# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
font pango:DejaVu Sans Mono 9
#font pango:Sauce Code for Powerline 10
#font pango:Courier 10
#font pango:Arial 10

new_window 1pixel
popup_during_fullscreen smart
workspace_auto_back_and_forth yes

for_window [title="Authy"] floating enable
for_window [title="^Google Hangouts"] floating enable
for_window [class="Xfce4-notifyd"] floating enable
for_window [class="jetbrains-idea-ce"] floating disable
for_window [instance="zoom"] floating enable
for_window [instance="TeamViewer"] floating enable
for_window [class="TeamViewer.exe"] floating enable
for_window [title="^TeamViewer Panel" instance="(?i)(?!google-chrome)$"] floating enable, move absolute position 900 px 0 px
for_window [title="TeamViewer" instance="^(?i)(?!google-chrome)$"] floating enable, move absolute position 0 px 0 px

#exec --no-startup-id which fluxgui && fluxgui &
#exec --no-startup-id which xscreensaver && xscreensaver &
#exec --no-startup-id which gpaste-client && gpaste-client applet

exec --no-startup-id which synsei && synsei load default
exec --no-startup-id which xbindkeys && xbindkeys
exec --no-startup-id which nm-applet && nm-applet &
exec --no-startup-id which redshift-gtk && redshift-gtk
exec --no-startup-id which connman-notify && connman-notify &
exec --no-startup-id which unclutter && unclutter -jitter 10 -idle 1 &
exec --no-startup-id which compton && compton -b
exec --no-startup-id which dunst && dunst &
exec --no-startup-id which nitrogen && nitrogen --restore
exec --no-startup-id which parcellite && parcellite &
exec --no-startup-id which caffeine-indicator && caffeine-indicator &
#exec --no-startup-id which xautolock && xautolock -time 1 -locker $lock &
exec --no-startup-id which xss-lock && xss-lock --notifier=/usr/local/libexec/xsecurelock/dimmer --transfer-sleep-lock -- $lock &

# exec programs
bindsym $ctrl+$shift+$alt+s exec slack
bindsym $ctrl+$shift+$alt+f exec firefox
bindsym $ctrl+$shift+$alt+c exec google-chrome

bindsym $ctrl+$shift+$alt+l exec --no-startup-id "$HOME/lenovo.sh -l"
bindsym $ctrl+$shift+$alt+r exec --no-startup-id "$HOME/lenovo.sh -r"
bindsym $ctrl+$shift+$alt+a exec --no-startup-id "$HOME/lenovo.sh -a"
bindsym $ctrl+$shift+$alt+n exec --no-startup-id "$HOME/nuc.sh"

set "$apps" "launch with apps"
bindsym $mod+o mode "$apps"
mode "$apps" {
  bindsym $ctrl+$shift+$alt+l exec --no-startup-id "$HOME/lenovo.sh -l -p"
  bindsym $ctrl+$shift+$alt+r exec --no-startup-id "$HOME/lenovo.sh -r -p"
  bindsym $ctrl+$shift+$alt+a exec --no-startup-id "$HOME/lenovo.sh -a -p"
  bindsym $ctrl+$shift+$alt+n exec --no-startup-id "$HOME/nuc.sh -p"

  bindsym Escape mode "default"
  bindsym Return mode "default"
}

# session commands
bindsym $ctrl+$alt+l exec "xset s activate"

# Move the current workspace to other outputs
bindsym $ctrl+$mod+$shift+Up move workspace to output up
bindsym $ctrl+$mod+$shift+Down move workspace to output down
bindsym $ctrl+$mod+$shift+Left move workspace to output left
bindsym $ctrl+$mod+$shift+Right move workspace to output right

# Layout a grid (g)
bindsym $mod+g split h, focus down, move up, focus down

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod
floating_minimum_size 75 x 50

# start a terminal
bindsym $ctrl+$alt+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+q kill

# There also is the (new) i3-dmenu-desktop which only displays applications shipping a desktop
# file. It is a wrapper around dmenu, so you need that installed.
bindsym $alt+space exec --no-startup-id "/usr/bin/rofi -show run"

# Change window focus with VIM-style keybindings
bindsym $mod+h focus output left
bindsym $mod+j focus output down
bindsym $mod+k focus output up
bindsym $mod+l focus output right

bindsym $mod+bracketleft focus left
bindsym $mod+bracketright focus right

bindsym $mod+$shift+Left focus left
bindsym $mod+$shift+Down focus down
bindsym $mod+$shift+Up focus up
bindsym $mod+$shift+Right focus right

# Move windows with VIM-style keybindings
bindsym $mod+$shift+h move left
bindsym $mod+$shift+j move down
bindsym $mod+$shift+k move up
bindsym $mod+$shift+l move right

# split in horizontal orientation
bindsym $mod+backslash split h

# split in vertical orientation
bindsym $mod+minus split v

# enter fullscreen mode for the focused container
bindsym $ctrl+$alt+$shift+Return fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+$ctrl+space floating toggle

# sticky the focused container on top
# requires i3 4.11+
bindsym $mod+$shift+s sticky toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+d focus child

# switch to workspace
bindsym $ctrl+$alt+1 workspace 1
bindsym $ctrl+$alt+2 workspace 2
bindsym $ctrl+$alt+3 workspace 3
bindsym $ctrl+$alt+4 workspace 4
bindsym $ctrl+$alt+5 workspace 5

# move focused container to workspace
bindsym $ctrl+$alt+$shift+1 move container to workspace 1
bindsym $ctrl+$alt+$shift+2 move container to workspace 2
bindsym $ctrl+$alt+$shift+3 move container to workspace 3
bindsym $ctrl+$alt+$shift+4 move container to workspace 4
bindsym $ctrl+$alt+$shift+5 move container to workspace 5

# focus workspace
bindsym $mod+Left workspace prev_on_output
bindsym $mod+Right workspace next_on_output

# reload the configuration file
bindsym $mod+$shift+r reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+$ctrl+$shift+r restart

# exit i3 (logs you out of your X session)
bindsym $mod+$shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize shrink height 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# volume controls
bindsym XF86AudioRaiseVolume exec amixer -q set Master 3.5dB+ unmute
bindsym XF86AudioLowerVolume exec amixer -q set Master 3.5dB- unmute
bindsym XF86AudioMute exec amixer -q set Master toggle

# Sreen brightness controls
# THUPER BROKEN
#bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
#bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness
bindsym XF86MonBrightnessUp exec echo $(($(cat /sys/class/backlight/$(ls /sys/class/backlight)/brightness) + 25)) | sudo tee /sys/class/backlight/$(ls /sys/class/backlight)/brightness
bindsym XF86MonBrightnessDown exec echo $(($(cat /sys/class/backlight/$(ls /sys/class/backlight)/brightness) - 25)) | sudo tee /sys/class/backlight/$(ls /sys/class/backlight)/brightness

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    position bottom
    mode hide
    hidden_state hide
    modifier $mod
    status_command $HOME/bin/i3status

    colors {
        background #1c1c1c
        statusline #ffffff
        separator #656565

        # Class             Border  BG      Text
        focused_workspace   #888888 #1c1c1c #eaeaea
        active_workspace    #333333 #5f676a #ffffff
        inactive_workspace  #333333 #222222 #888888
        urgent_workspace    #2f343a #ffc123 #ffffff
    }
}

#border     - titlebar border color (NA)
#backgr     - window border
#text       - titlebar text color (NA)
#indicator  - layout type indicator

# class                 border  backgr. text    indicator
client.focused          #222222 #6272a4 #ffffff #ffffff
client.focused_inactive #333333 #222222 #ffffff #888888
client.unfocused        #333333 #222222 #888888 #888888
client.urgent           #222222 #ffc123 #ffffff #ffc123

# window title
for_window [title=".*"] title_format %title | %instance | %class
