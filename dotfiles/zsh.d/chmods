CHMOD="chmod -f"

if [ -f $HOME/.gnupg ]
then
  $CHMOD 700 $HOME/.gnupg
fi

if [ -f $HOME/.fetchmailrc ]
then
  $CHMOD 600 $HOME/.fetchmailrc
fi

if [ -f $HOME/.muttrc ]
then
  $CHMOD 640 $HOME/.muttrc
fi
